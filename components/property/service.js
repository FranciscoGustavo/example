/**
 * Find properties apply default values
 * limit 
 * sort
 * skip
 * @param {object} query 
 */
const findAll = async(query) => {

}

/**
 * Iner a new property in db
 * @param {*} properyData 
 */
const insertOne = (properyData) => {

}

module.exports = {
  findAll,
  insertOne
};
